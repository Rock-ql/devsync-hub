<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.devsync.mapper.WorkItemLinkMapper">

    <!-- 根据关联类型和关联ID列表批量查询关联记录 -->
    <select id="selectByLinkTypeAndLinkIds" resultType="com.devsync.entity.WorkItemLink">
        SELECT * FROM work_item_link
        WHERE link_type = #{linkType}
          AND deleted_at IS NULL
          AND link_id IN
        <foreach collection="linkIds" item="linkId" open="(" separator="," close=")">
            #{linkId}
        </foreach>
    </select>

</mapper>
